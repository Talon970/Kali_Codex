Kali-Linux

Commands:

# shutdown PC after XY
sudo shutdown -h +60

# shutdown interrupt
sudo shutdown -c

# shows the full path of the directory
pwd

Weekly:

# 1. Paketquellen aktualisieren
sudo apt update

# 2. Upgrade aller installierten Pakete
sudo apt upgrade -y

# update or upgrade the system
sudo apt update && sudo apt upgrade -y

# 4. Autoremove (Aufräumen)
sudo apt autoremove -y

Clean System:

# 1. System aktualisieren
sudo apt update && sudo apt upgrade -y

# 2. Alte Pakete und Konfigurationen entfernen
sudo apt autoremove --purge

# 3. Paket-Cache leeren
sudo apt clean

# 4. (Optional) System-Logs verkleinern
sudo journalctl --vacuum-size=200M
